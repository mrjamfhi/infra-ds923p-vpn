all:
  hosts:
    synology:
      ansible_host: 10.0.4.2
      ansible_user: your_admin_user
      ansible_become_password: "{{ vault_synology_become_password }}"
      # Or use SSH key:
      # ansible_ssh_private_key_file: ~/.ssh/id_synology
